{
  "name": "azure",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --coverage --coverageDirectory __tests__/coverage --config jest.config.json"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "mongodb": "^4.5.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "jest": "^28.1.0",
    "typescript": "^4.0.0"
  }
}
